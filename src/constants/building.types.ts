export type Floor = {
    id: number; name: string
};

export type Office = {
    id: number; name: string
};

export type Room = {
    id: number; name: string
};

export type Device = {
    id: number; name: string; status: string
};